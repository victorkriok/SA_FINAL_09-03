para rodar o swagger e testar os end points, rode dotnet run

abra o localhost com o seguinte URL http://localhost:5269/swagger.

Se a porta estiver diferente de 5269 (Oque nao deveria acontecer) apenas adicione /swagger no final da URL